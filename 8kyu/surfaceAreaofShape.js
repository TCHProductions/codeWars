/*function getSize(w, h, d){
  let empArr = []
  empArr.push((d * h *2) + (w*h*2) + (d*w*2) )
  empArr.push(w * h * d)
  return empArr
}
*/

function getSize(w, h, d){
    let empArr = []
    empArr.push((d * h *2) + (w*h*2) + (d*w*2) )
    empArr.push(w * h * d)
    return empArr
  }
  